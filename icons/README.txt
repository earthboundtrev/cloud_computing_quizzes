Add PWA icons here for "Add to Home Screen" and app install:

- icon-192.png   (192×192 px)
- icon-512.png   (512×512 px)

Use the same design (e.g. "Cram" logo or a book/flame). PNG format.
If these files are missing, the app still works offline; install may show a generic icon.
